version: '3.8'

services:
  app:
    build: .
    container_name: student_management_app
    ports:
      - "5000:5000"
    environment:
      - FLASK_APP=run.py
      - FLASK_ENV=development
    volumes:
      - .:/app 
    restart: always
